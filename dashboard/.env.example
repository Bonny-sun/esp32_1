# Copy to  dashboard/.env  for local runs (loaded via python-dotenv).
# On Render these are set as real env vars. Gitignored — never commit.

SUPABASE_URL=https://bwqvgiptnkzrsogpgvbq.supabase.co
SUPABASE_SERVICE_KEY=sb_secret_xxxxxxxx

# Optional: if set, the threshold editor requires this password.
# Leave empty for an open editor (local dev only).
DASH_PASSWORD=
